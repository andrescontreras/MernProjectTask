import React from 'react';
import PropTypes from 'prop-types';

const Task = props => {
    return (
        <div>
            <p>Desde tarea</p>
        </div>
    );
};

Task.propTypes = {
    
};

export default Task;